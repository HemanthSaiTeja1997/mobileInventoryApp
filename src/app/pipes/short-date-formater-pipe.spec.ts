import { ShortDateFormaterPipe } from './short-date-formater-pipe';

describe('ShortDateFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortDateFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
